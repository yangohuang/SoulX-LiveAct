# 416×720 two-step quality/speed frontier

Question: On one RTX 4090, is the existing two-step LiveAct schedule a defensible 30-second speed/quality trade-off for a streaming digital human, compared with three steps at the *same* latent resolution?

Use identity 2, seed 43, first 30 seconds of its exact archived PCM audio (SHA-256 `85372c99f11ca8396d9c01bb1aa1314bb165e93000938ba87f8ad06de104637e`), prompt `一个人在说话`, 416×720/24 FPS, FP8 GEMM/KV, CPU block/cache offload, T5/prompt and FP8 caches, no compile and no latent anchor. Keep resident KV steps at **0** to match the archived three-step baseline. Generate fresh three-step and two-step runs in the same checkout with `--dura_print`, so block-time comparisons use matched code/environment rather than old uncontrolled logs. Both must decode to the same 722 video frames and 30 seconds of audio. The schedule removes the middle timestep (937.5) and changes the number of audio-conditioned DiT passes from two to one; it is an algorithmic quality change, not only an implementation optimization.

Predeclare a provisional pass only if median later-block cost improves at least 20%; mean eight-transition boundary motion sum remains at least 90% of fresh three-step; mean eight-transition peak does not exceed 125% of fresh baseline; SyncNet relative confidence falls by no more than 0.30 with comparable face-crop endpoint and stable best offset; once-per-second single-face detection remains at least 90% and mean reference-face cosine falls by no more than 0.03; and visual review of the five worst boundary windows plus at least seven whole-video timepoints shows no conspicuous new hand/face/mouth defect. If any gate fails, reject two steps as a quality-preserving default and stop before held-out identities. If all pass, run identity 1 and 3 at seed 43 and apply the same gates against their archived three-step baselines, disclosing the missing fresh timing controls on holdouts.

Use separate-run comparisons, not pixel-aligned counterfactual language. A score pass in one identity is not proof of natural motion, interaction latency, or 24-FPS real-time generation. Do not send a SoulX official PR without user review; sync research to the personal fork.
